apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "intelligence.fullname" . }}-config
  labels:
    {{- include "intelligence.labels" . | nindent 4 }}
data:
  config.yaml: |
    {{- toYaml .Values.config | nindent 4 }}
